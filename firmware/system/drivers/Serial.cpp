#include "Serial.h"

#include "../1923VK014.h"

// TODO methods

bool Serial::close() { return true; }

bool Serial::open() { return true; }

int Serial::read(char* data, int size) { return 0; }

void Serial::flush() {}

int Serial::write(const char* data, int size) { return 0; }
